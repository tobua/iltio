import { CSSProperties } from 'react'

export type Variables = {
  color?: string
  contrast?: string
  borderRadius?: number
}

export type Styles = {
  form?: CSSProperties
  inputMail?: CSSProperties
  button?: CSSProperties
  tab?: CSSProperties
}
