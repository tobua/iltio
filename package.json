{
  "name": "iltio-main",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "cd plugin && bun run build",
    "update": "workspaces \"update --no-install\" --output"
  },
  "type": "module",
  "dependencies": {
    "ajv": "^7"
  },
  "trustedDependencies": [
    "papua"
  ],
  "workspaces": [
    "demo/*",
    "demo/frameworks/*",
    "plugin",
    "test"
  ]
}
